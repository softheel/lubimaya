# Локальная разработка — скопируйте в .env и подставьте свои значения.
# На VPS создайте файл backend/.env по этому шаблону.

DB_HOST=localhost
DB_USER=lubimaya_user
DB_PASSWORD=НадёжныйПароль123!
DB_NAME=lubimaya
PORT=5001
JWT_SECRET=замените-на-длинную-случайную-строку
